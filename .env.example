# Arbitrum Sepolia (chain id 421614)
RPC_URL=https://sepolia-rollup.arbitrum.io/rpc
# A throwaway testnet key only. Never commit a real key.
PRIVATE_KEY=0xyour_test_key_here
# One Etherscan API key (etherscan.io) — works for Arbiscan via the v2 API.
ETHERSCAN_API_KEY=your_etherscan_api_key
# Filled in after you deploy:
ANCHOR_ADDRESS=0xyour_deployed_contract
